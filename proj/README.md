## Setup Instructions

1. Install Docker and Docker Compose if not already installed
2. Navigate to the project directory
3. Run `docker-compose up -d` to start ScyllaDB
4. Run the application with `[your application start command]`
5. To stop the database: `docker-compose down`
